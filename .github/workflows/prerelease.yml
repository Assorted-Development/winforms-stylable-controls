# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET Pre-Release

on:
  push:
    # just for testing - switch to main before merging
    branches: [ "wolframhaussig-patch-auto-release" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    - name: Debug Output
      run: dir
    - name: Restore dependencies
      run: dotnet restore ${{ github.workspace }}/StylableWinFormsControls/StylableWinFormsControls.sln
    - name: Build
      run: dotnet build ${{ github.workspace }}/StylableWinFormsControls --no-restore

    - uses: "marvinpinto/action-automatic-releases@latest"
      with:
        repo_token: "${{ secrets.GITHUB_TOKEN }}"
        automatic_release_tag: "latest"
        prerelease: true
        title: "Development Build"
        files: |
          LICENSE.txt
          StylableWinFormsControls/StylableWinFormsControls/bin/Debug/net6.0-windows/StylableWinFormsControls.dll
          *.nupkg
          *.snupkg
      
      
